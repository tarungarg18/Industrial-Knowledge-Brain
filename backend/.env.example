# Copy this file to .env and fill in your values
# Run `lemma pod list` to get your pod ID
# Run `lemma auth print-token` to get a token (expires in ~1hr; regenerate as needed)

LEMMA_POD_ID=your-pod-id-here
LEMMA_TOKEN=your-lemma-token-here
LEMMA_BASE_URL=https://api.lemma.work
